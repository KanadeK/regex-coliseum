# Benchmark

Benchmark command: `npm run demo`. It evaluates the shipped Email Extractor fixture plus the challenge’s deterministic performance corpus. Record the actual device, Node/browser runtime, case count, and elapsed output during the release gate; benchmark claims are intentionally not prefilled.
