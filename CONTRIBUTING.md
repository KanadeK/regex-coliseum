# Contributing

Use Node LTS, run `npm ci`, and keep challenge fixtures deterministic and offline. Add a failing test before fixing a defect, then run `make verify`. New challenges must include visible, hidden, and performance data, and must not expose hidden inputs through the sharing adapter. Submit focused pull requests using the supplied template.
